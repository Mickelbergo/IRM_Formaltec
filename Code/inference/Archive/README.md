# Archive

This directory contains archived/outdated inference scripts.

## Archived Files

- **generate_images_dummy.py** - Basic unconditional generation script (no mask conditioning)

## Why Archived?

These files are kept for reference but are no longer actively used.

**Current implementations:**
- Use `generate_images.py` for mask-conditional image generation
- Use `../training/generate_synthetic.py` for batch synthetic image generation
