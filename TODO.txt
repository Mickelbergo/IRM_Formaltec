1. fix the swin transformer architecture (might add some new layers, or some skip connections, look at chatgpt from last week)

2. refine object detection -> train a object detection model instead of just using a pretrained one
