using weighted cross entropy and also oversampling leads to good results on the training data (for the transformer model) but performs horribly on validation data
check why that is

for that, maybe use 2 different weights function

also, some classes still have an iou score of 0, check why that is 